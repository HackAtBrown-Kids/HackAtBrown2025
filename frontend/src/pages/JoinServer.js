import Header from '../components/Header';

const JoinServer = () => {
  return (
    <div>
      <Header />
      <h2>Join a server</h2>
      <input>Server code</input>
      <button
    </div>
  );
};

export default JoinServer;